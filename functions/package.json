{
  "name": "habitual-functions",
  "description": "Cloud Functions for Habitual app (recommendations, background tasks)",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "main": "lib/index.js",
  "private": true
}
